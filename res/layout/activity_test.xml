<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".TestActivity" >

    <Button
        android:id="@+id/btnDepTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_test_dep_titles" />

    <EditText
        android:id="@+id/etTitles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/btnDepTitle"
        android:hint="@string/titles_hint"
        android:inputType="text" />
    
    <Button
        android:id="@+id/btnDepSerId"
        android:layout_below="@+id/btnDepTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_test_dep_ids" />

    <EditText
        android:id="@+id/etIds"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/btnDepSerId"
        android:layout_below="@+id/etTitles"
        android:hint="@string/ids_hint"
        android:inputType="text" />

</RelativeLayout>